# Generated by Next.js on every build — formatting it is pointless and makes the
# format gate flap depending on whether a build has run since the last format.
next-env.d.ts
.next/
out/
build/
node_modules/
package-lock.json
