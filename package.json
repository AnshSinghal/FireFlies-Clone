{
  "name": "fireflies-clone",
  "private": true,
  "description": "Tooling-only root package. Owns the git hooks for a polyglot monorepo; the apps live in frontend/ and backend/.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2"
  }
}
